<Window x:Class="Banga_Photobooth.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Banga_Photobooth"
        mc:Ignorable="d"
        Title="Admin Settings" Height="850" Width="1300"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA"
        WindowState="Maximized"
        FontFamily="Assets/Fonts/#Poppins">

    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="PrimaryColor">#2563EB</Color> <!-- Royal Blue -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1E40AF"/>
        <SolidColorBrush x:Key="TextDarkBrush" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextLightBrush" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F3F4F6"/>

        <!-- Styles -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="25,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0,8,8,0" Margin="0,2,15,2">
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#111827"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActiveSidebarButtonStyle" TargetType="Button" BasedOn="{StaticResource SidebarButtonStyle}">
            <Setter Property="Background" Value="#EFF6FF"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="4,0,0,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" BlurRadius="15" ShadowDepth="4" Opacity="0.03"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
        
        <Style x:Key="FormInput" TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Effect="{TemplateBinding Effect}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#9CA3AF"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                         <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F9FAFB"/>
                                <Setter Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ImageUploadBox" TargetType="Button">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="200"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Rectangle x:Name="border" 
                                     Fill="{TemplateBinding Background}"
                                     Stroke="{TemplateBinding BorderBrush}"
                                     StrokeThickness="1"
                                     StrokeDashArray="4 2"
                                     RadiusX="8" RadiusY="8"
                                     SnapsToDevicePixels="True"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Stroke" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="Fill" Value="#EFF6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo / Brand -->
                <StackPanel Grid.Row="0" Margin="30,40,30,40">
                    <TextBlock Text="BANGA" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="ADMIN PANEL" FontSize="14" FontWeight="Medium" Foreground="#6B7280" Margin="2,-5,0,0"/>
                </StackPanel>

                <!-- Navigation -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="MENU" FontSize="12" FontWeight="Bold" Foreground="#9CA3AF" Margin="30,0,0,10"/>
                    <Button x:Name="BtnTemplates" Content="Templates" Click="SidebarButton_Click" Tag="Templates" Style="{StaticResource ActiveSidebarButtonStyle}"/>
                    <Button x:Name="BtnConfig" Content="Configuration" Click="SidebarButton_Click" Tag="Config" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button x:Name="BtnAppearance" Content="Appearances" Click="SidebarButton_Click" Tag="Appearance" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button x:Name="BtnAbout" Content="About App" Click="SidebarButton_Click" Tag="About" Style="{StaticResource SidebarButtonStyle}"/>
                </StackPanel>

                <!-- Bottom Actions -->
                <StackPanel Grid.Row="2" Margin="20">
                    <Button Content="Exit Settings" Click="ExitButton_Click" Style="{StaticResource SecondaryButton}" Foreground="#EF4444" BorderBrush="#FECACA" Height="45">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1" Background="#F9FAFB">
            
            <!-- Templates Page -->
            <Grid x:Name="TemplatesPage" Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Page Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,30">
                    <TextBlock Text="Template Management" FontSize="28" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="Manage, add, or edit your photobooth templates." FontSize="16" Foreground="#6B7280" Margin="0,5,0,0"/>
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Column: List & Add Button -->
                    <Grid Grid.Column="0" Margin="0,0,30,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Button x:Name="BtnAddNew" Click="BtnAddNew_Click" Style="{StaticResource PrimaryButton}" Height="50" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="+" FontSize="18" Margin="0,0,10,0"/>
                                <TextBlock Text="Add New Template" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="15">
                                    <TextBlock Text="Existing Templates" FontWeight="Bold" Foreground="#374151"/>
                                </Border>
                                <ListBox x:Name="TemplatesListBox" Grid.Row="1" BorderThickness="0" Margin="5" 
                                         SelectionChanged="TemplatesListBox_SelectionChanged"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Padding" Value="10,12"/>
                                            <Setter Property="Margin" Value="0,2"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="bd" Property="Background" Value="#F3F4F6"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="bd" Property="Background" Value="#EFF6FF"/>
                                                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Right Column: Editor Form -->
                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Form Header -->
                            <Border Grid.Row="0" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="25,20">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="Editor: " FontSize="18" FontWeight="Bold" Foreground="#374151"/>
                                    <TextBlock x:Name="TxtEditorTitle" Text="New Template" FontSize="18" FontWeight="Normal" Foreground="{StaticResource PrimaryBrush}" Margin="5,0,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Scrollable Content -->
                            <ScrollViewer Grid.Row="1" Padding="25,20" VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <!-- Basic Info Section -->
                                    <TextBlock Text="Basic Information" FontSize="16" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,15"/>
                                    
                                    <TextBlock Text="Template Name" Style="{StaticResource FormLabel}"/>
                                    <TextBox x:Name="TxtTemplateName" Style="{StaticResource FormInput}" Margin="0,0,0,20" 
                                             Tag="Unique name (e.g. Summer_Vibes)"/>

                                    <!-- Images Section -->
                                    <TextBlock Text="Assets" FontSize="16" FontWeight="Bold" Foreground="#111827" Margin="0,10,0,15"/>
                                    
                                    <Grid Margin="0,0,0,20">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Display Image -->
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Display Preview (UI)" Style="{StaticResource FormLabel}"/>
                                            <Button x:Name="BtnSelectDisplay" Click="BtnSelectDisplayImage_Click" Style="{StaticResource ImageUploadBox}">
                                                <Grid>
                                                    <StackPanel x:Name="DisplayPlaceholder" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock Text="🖼️" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                                        <TextBlock Text="Click to Upload" Foreground="#6B7280"/>
                                                        <TextBlock Text="(.png)" FontSize="12" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                    <Image x:Name="ImgDisplayPreview" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                                                </Grid>
                                            </Button>
                                            <TextBlock Text="Will be saved as [Name] double.png" FontSize="12" Foreground="#9CA3AF" Margin="0,5,0,0"/>
                                        </StackPanel>

                                        <!-- Use Image -->
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Print Template (Actual)" Style="{StaticResource FormLabel}"/>
                                            <Button x:Name="BtnSelectUse" Click="BtnSelectUseImage_Click" Style="{StaticResource ImageUploadBox}">
                                                <Grid>
                                                    <StackPanel x:Name="UsePlaceholder" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <TextBlock Text="🖨️" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                                        <TextBlock Text="Click to Upload" Foreground="#6B7280"/>
                                                        <TextBlock Text="(.png)" FontSize="12" Foreground="#9CA3AF" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                    <Image x:Name="ImgUsePreview" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                                                </Grid>
                                            </Button>
                                            <TextBlock Text="Will be saved as [Name].png" FontSize="12" Foreground="#9CA3AF" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Metadata Section -->
                                    <TextBlock Text="Layout Configuration (Metadata)" FontSize="16" FontWeight="Bold" Foreground="#111827" Margin="0,10,0,15"/>
                                    
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Width (px)" Style="{StaticResource FormLabel}"/>
                                            <TextBox x:Name="TxtWidth" Style="{StaticResource FormInput}" PreviewTextInput="NumberValidationTextBox"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Height (px)" Style="{StaticResource FormLabel}"/>
                                            <TextBox x:Name="TxtHeight" Style="{StaticResource FormInput}" PreviewTextInput="NumberValidationTextBox"/>
                                        </StackPanel>
                                    </Grid>

                                    <TextBlock Text="Photo Slots JSON" Style="{StaticResource FormLabel}"/>
                                    <TextBox x:Name="TxtPhotoSlots" Height="120" TextWrapping="Wrap" AcceptsReturn="True" 
                                             Style="{StaticResource FormInput}" VerticalContentAlignment="Top" Padding="12"
                                             FontFamily="Consolas, Courier New" FontSize="13" Margin="0,0,0,15"/>

                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="QR Slot JSON" Style="{StaticResource FormLabel}"/>
                                            <TextBox x:Name="TxtQrSlot" Style="{StaticResource FormInput}" FontFamily="Consolas, Courier New" FontSize="13"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Boundary JSON" Style="{StaticResource FormLabel}"/>
                                            <TextBox x:Name="TxtBoundary" Style="{StaticResource FormInput}" FontFamily="Consolas, Courier New" FontSize="13"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>

                            <!-- Form Actions -->
                            <Border Grid.Row="2" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="25,20" Background="#F9FAFB" CornerRadius="0,0,12,12">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Reset / Clear" Click="BtnClear_Click" Style="{StaticResource SecondaryButton}" Width="120" Margin="0,0,15,0"/>
                                    <Button Content="Save Template" Click="BtnSaveTemplate_Click" Style="{StaticResource PrimaryButton}" Width="150"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
            
            <!-- Configuration Page -->
            <Grid x:Name="ConfigPage" Visibility="Collapsed" Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,30">
                    <TextBlock Text="Hardware Configuration" FontSize="28" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="Manage your camera, printer, and simulation settings." FontSize="16" Foreground="#6B7280" Margin="0,5,0,0"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Mode Selection -->
                        <Border Style="{StaticResource CardStyle}" Padding="25,20" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Operation Mode" FontSize="18" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>
                                
                                <!-- Simulation Description can go here if needed -->
                            </StackPanel>
                        </Border>

                        <!-- Camera Settings -->
                        <Border Style="{StaticResource CardStyle}" Padding="25,20" Margin="0,0,0,20">
                            <StackPanel>
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Camera Settings" FontSize="18" FontWeight="Bold" Foreground="#374151"/>
                                    
                                    <!-- Help Button -->
                                    <Button x:Name="BtnCameraHelp" Click="BtnHelp_Click" Width="30" Height="30" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right">
                                        <Grid>
                                            <Ellipse Fill="#F59E0B" Width="24" Height="24"/>
                                            <TextBlock Text="!" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                        <Button.ToolTip>
                                            <ToolTip Content="Click for Plug &amp; Play Setup Guide"/>
                                        </Button.ToolTip>
                                    </Button>
                                </Grid>

                                <!-- Camera Simulation Toggle -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="Use Hardware Camera (Canon)" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                                    <ToggleButton x:Name="TglCameraSimulation" Width="50" Height="26" Checked="TglCameraSimulation_Checked" Unchecked="TglCameraSimulation_Checked">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="border" CornerRadius="13" Background="#E5E7EB" BorderBrush="#D1D5DB" BorderThickness="1">
                                                    <Ellipse x:Name="dot" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="2"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="dot" Property="HorizontalAlignment" Value="Right"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <TextBlock Text="Use Webcam (Sim)" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#4B5563"/>
                                </StackPanel>

                                <!-- Webcam Selection (Visible if Sim ON) -->
                                <StackPanel x:Name="WebcamPanel" Visibility="Collapsed">
                                    <TextBlock Text="Select Webcam" Style="{StaticResource FormLabel}"/>
                                    <ComboBox x:Name="CboWebcams" Height="42" Padding="10,0" Margin="0,0,0,15" SelectionChanged="CboWebcams_SelectionChanged">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" VerticalAlignment="Center" Padding="5"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Canon Info (Visible if Sim OFF) -->
                                <StackPanel x:Name="CanonPanel" Visibility="Collapsed" Background="#F3F4F6" Margin="0,0,0,15">
                                    <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="15">
                                        <StackPanel>
                                            <TextBlock Text="Canon EOS 2000D Selected" FontWeight="Bold" Foreground="#374151"/>
                                            <TextBlock Text="No Driver Required (Plug &amp; Play)" FontSize="12" Foreground="#10B981" Margin="0,5,0,0"/>
                                            <TextBlock Text="Ensure camera is in 'Movie Mode' or 'PC Connect' mode." FontSize="12" Foreground="#6B7280"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <Separator Margin="0,5,0,15"/>
                                
                                <!-- Invert Camera Toggle -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="Invert Camera (Mirror)" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                                    <ToggleButton x:Name="TglInvertCamera" Width="50" Height="26" Checked="TglInvertCamera_Changed" Unchecked="TglInvertCamera_Changed">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="border" CornerRadius="13" Background="#E5E7EB" BorderBrush="#D1D5DB" BorderThickness="1">
                                                    <Ellipse x:Name="dot" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="2"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="dot" Property="HorizontalAlignment" Value="Right"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <TextBlock Text="(Flip horizontally)" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#9CA3AF" FontSize="12"/>
                                </StackPanel>
                                
                                <Separator Margin="0,5,0,15"/>
                                <TextBlock Text="Countdown Timer (Seconds)" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="TxtCountdown" Style="{StaticResource FormInput}" Width="100" HorizontalAlignment="Left" PreviewTextInput="NumberValidationTextBox" TextChanged="TxtCountdown_TextChanged"/>
                            </StackPanel>
                        </Border>

                        <!-- Printer Settings -->
                        <Border Style="{StaticResource CardStyle}" Padding="25,20">
                            <StackPanel>
                                <TextBlock Text="Printer Settings" FontSize="18" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>

                                <!-- Printer Simulation Toggle -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="Use Hardware Printer" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                                    <ToggleButton x:Name="TglPrinterSimulation" Width="50" Height="26" Checked="TglPrinterSimulation_Checked" Unchecked="TglPrinterSimulation_Checked">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="border" CornerRadius="13" Background="#E5E7EB" BorderBrush="#D1D5DB" BorderThickness="1">
                                                    <Ellipse x:Name="dot" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="2"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="dot" Property="HorizontalAlignment" Value="Right"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <TextBlock Text="Simulation (File Save)" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#4B5563"/>
                                </StackPanel>

                                <!-- Printer Selection (Visible if Sim OFF) -->
                                <StackPanel x:Name="PrinterPanel" Visibility="Collapsed">
                                    <!-- Info Box -->
                                    <Border Background="#FEF3C7" BorderBrush="#F59E0B" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="⚠️ Two Printer Profiles Required" FontWeight="Bold" Foreground="#92400E" Margin="0,0,0,5"/>
                                            <TextBlock Text="• Strip Printer: Set Paper to '4x6 (2 inch cut)' in driver" FontSize="12" Foreground="#78350F"/>
                                            <TextBlock Text="• 4R Printer: Set Paper to '4x6' (No Cut) in driver" FontSize="12" Foreground="#78350F"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Strip Mode Printer -->
                                    <TextBlock Text="Strip Mode Printer (600x1800 - Cut ON)" Style="{StaticResource FormLabel}"/>
                                    <ComboBox x:Name="CboPrinterStrip" Height="42" Padding="10,0" Margin="0,0,0,15" SelectionChanged="CboPrinterStrip_SelectionChanged">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" VerticalAlignment="Center" Padding="5"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    
                                    <!-- 4R Mode Printer -->
                                    <TextBlock Text="4R Mode Printer (1200x1800 - Cut OFF)" Style="{StaticResource FormLabel}"/>
                                    <ComboBox x:Name="CboPrinter4R" Height="42" Padding="10,0" Margin="0,0,0,15" SelectionChanged="CboPrinter4R_SelectionChanged">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" VerticalAlignment="Center" Padding="5"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                    
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="TxtPrinterStatus" Text="Status: Unknown" FontSize="14" Foreground="#6B7280" VerticalAlignment="Center"/>
                                        <Button x:Name="BtnCheckPrinter" Content="Refresh Printers" Click="BtnCheckPrinter_Click" Style="{StaticResource SecondaryButton}" Height="35"/>
                                    </Grid>
                                </StackPanel>
                                
                                <!-- File Save Info (Visible if Sim ON) -->
                                <StackPanel x:Name="FileSavePanel" Visibility="Collapsed" Background="#F3F4F6" Margin="0,0,0,15">
                                    <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="15">
                                        <StackPanel>
                                            <TextBlock Text="Simulation Mode Active" FontWeight="Bold" Foreground="#374151"/>
                                            <TextBlock Text="Prints will be saved to 'Documents/Banga_Prints'" FontSize="12" Foreground="#6B7280" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Appearances Page -->
            <Grid x:Name="AppearancePage" Visibility="Collapsed" Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,30">
                    <TextBlock Text="Appearances" FontSize="28" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="Customize the look and feel of the application." FontSize="16" Foreground="#6B7280" Margin="0,5,0,0"/>
                </StackPanel>

                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="25,20" VerticalAlignment="Top">
                    <StackPanel>
                        <TextBlock Text="Font Settings" FontSize="18" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Application Font Family" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="CboFonts" Height="42" Padding="10,0" Margin="0,0,0,5" SelectionChanged="CboFonts_SelectionChanged">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Source}" FontFamily="{Binding}" VerticalAlignment="Center" Padding="5" FontSize="14"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <TextBlock Text="Note: Changes will apply after restarting the application." FontSize="12" Foreground="#F59E0B" Margin="0,5,0,0"/>
                        
                        <Separator Margin="0,20"/>
                        
                        <TextBlock Text="Preview Text:" Style="{StaticResource FormLabel}"/>
                        <Border Background="#F3F4F6" Padding="15" CornerRadius="6">
                            <StackPanel>
                                <TextBlock x:Name="TxtFontPreview" Text="The quick brown fox jumps over the lazy dog." FontSize="18" Foreground="#374151" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtFontPreview2" Text="BANGA PHOTOBOOTH 2025" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- About App Page -->
            <Grid x:Name="AboutPage" Visibility="Collapsed" Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,30">
                    <TextBlock Text="About App" FontSize="28" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="Version information, licenses, and credits." FontSize="16" Foreground="#6B7280" Margin="0,5,0,0"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- App Info Card -->
                        <Border Style="{StaticResource CardStyle}" Padding="40" Margin="0,0,0,20">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="BANGA PHOTOBOOTH" FontSize="32" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="Version 1.0.0" FontSize="16" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,5,0,20"/>
                                
                                <TextBlock Text="2025 Copyright Banga Photobooth." FontSize="14" Foreground="#374151" HorizontalAlignment="Center"/>
                                <TextBlock Text="Proprietary Software." FontSize="14" Foreground="#374151" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                <TextBlock Text="Unauthorized copying, reverse engineering, or distribution is strictly prohibited." 
                                         FontSize="12" Foreground="#9CA3AF" HorizontalAlignment="Center" Margin="0,5,0,0" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="500"/>
                            </StackPanel>
                        </Border>

                        <!-- Libraries & Credits -->
                        <Border Style="{StaticResource CardStyle}" Padding="25,20">
                            <StackPanel>
                                <TextBlock Text="Open Source Libraries &amp; Licenses" FontSize="18" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>
                                <TextBlock Text="This software uses the following third-party libraries:" FontSize="14" Foreground="#6B7280" Margin="0,0,0,15"/>

                                <ItemsControl x:Name="LibrariesList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="0,10">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#374151" FontSize="14"/>
                                                    <TextBlock Grid.Row="1" Text="{Binding License}" Foreground="#6B7280" FontSize="12" Margin="0,2,0,0"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Placeholder (Removed) -->
            <Grid x:Name="PlaceholderPage" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
